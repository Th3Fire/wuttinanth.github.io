{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-router": "^4.1.2",
    "react-router-dom": "^4.1.2",
    "react-scripts": "1.0.10"
  },
  "scripts": {
    "start": "npm-run-all --parallel start-dev-api run-react",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "run-react": "react-scripts start",
    "start-dev-api": "json-server --watch api/db.json --routes api/routes.json --port 5000"
  },
  "devDependencies": {
    "json-server": "^0.11.2",
    "npm-run-all": "^4.0.2"
  }
}
